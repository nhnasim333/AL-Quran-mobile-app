import { Text, View } from "react-native";

const Listen = () => {
  return (
    <View className="flex-1 items-center justify-center">
      <Text className="text-3xl font-bold text-dark-200">
        Welcome to Listen Screen!
      </Text>
    </View>
  );
};
export default Listen;
